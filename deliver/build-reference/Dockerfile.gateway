# Образ шлюза SCADA (готовый fat-jar на JRE 21). Конфиг зашит в jar,
# адреса переопределяются через env (см. docker-compose.yml).
FROM eclipse-temurin:21-jre
WORKDIR /app
COPY app.jar /app/app.jar
EXPOSE 8888
ENTRYPOINT ["java","-jar","/app/app.jar"]
